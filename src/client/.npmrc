# Flat node_modules (pnpm): Angular + webpack resolve @angular/* reliably (fixes NG3004).
node-linker=hoisted

# Also hoist these peers to the root (safe with hoisted linker).
public-hoist-pattern[]=*@angular*
public-hoist-pattern[]=*rxjs*
public-hoist-pattern[]=*zone.js*
